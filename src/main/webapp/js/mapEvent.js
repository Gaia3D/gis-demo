(function() {
	// 지도 확대

	// 지도 축소

	// 지도 회전
	$('.rotate').on('click', function() {
		var direction = $(this).data('direction');
		HHI.Tools.setRotate(direction);
	});

	// 초기화

	// 블록 조회

	// 블록 지우기

	// 블록 이동
})();
